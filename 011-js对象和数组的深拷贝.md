深拷贝的场景多用于动态创建数据或对象
当然浅拷贝可以复制数组或的对象的地址，可以直接修改值而不需要重新删除并增加

我认为好用的

对象的深拷贝

```javascript
Object.defineProperty(obj, name, {
  value: {},
  writable: true,
  enumerable: true,
  configurable: true,
});
```

这个甚至做到动态命名，你可以循环然后修改name，再用这个增加对象。

数组的深拷贝

```javascript
[].slice();
```
