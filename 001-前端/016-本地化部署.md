1. 后端http服务开启
2. 第一次请求返回一个资源（一般是index.html）
3. 多次请求把剩余文件发送
4. 在前端，把后面请求到的字符串（http的响应数据都是字符串）转换成可用url访问的文件

5. 我真的是醉了,根本不是这么弄的,不用我专门先多次请求.你在标签(如<a></a>)中使用url本身就是一次网络请求,所以你只需要写好地址,若标签显示则触发请求(url+地址),然后后端根据后缀的地址返回整个文件,就可以了.
