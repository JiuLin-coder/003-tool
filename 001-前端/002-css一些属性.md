```CSS
html{font-size:10px;}

.mg20{
    /* 这个就会换算成10px */
    margin:1rem;
}

/* 只在该区域移动 */
/*
    px,rem的用法
    1px就是对于电脑的一个像素的大小。是绝对单位。
    1rem就是相对与html（最外层）的字体大小。
 */
{
    /* 必须有height， */
    height: 40rem;
    overflow:auto
}

```
